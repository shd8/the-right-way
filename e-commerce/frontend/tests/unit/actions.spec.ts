// import actions from '@/store/actions';

// const mockReturnValue = mockReturnValue as jest.Mock<any>;

// test('Products from API fetcher', () => {
//   const state = {
//     products: [{ name: '1' }, { name: '2' }],
//     commit: jest.fn(),
//   };
//   actions.fetchProductsFromApi(state).mockReturnValue('');

//   expect(state.products).toStrictEqual(['']);
// });
