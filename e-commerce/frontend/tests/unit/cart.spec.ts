// import { mount } from '@vue/test-utils';
// import Cart from '@/views/Cart.vue';

// test('displays message', () => {
//   const wrapper = mount(Cart);

//   // Assert the rendered text of the component
//   expect(wrapper.text()).toContain('Cart works');
// });
